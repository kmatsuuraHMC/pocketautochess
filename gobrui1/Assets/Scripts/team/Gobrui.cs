using UnityEngine;
using System.Collections;
using S = System;
using System.Collections.Generic;
using System.Linq;

public class Gobrui 
{

    // private void Start()
    // {
    //     hp = 4000;
    // }
    // private void Update()
    // {
    //     if (hp < 0) { Team.delete(myTeam.teamMates, this.number); }
    //     if (BackBoard.turn == BackBoard.Turn.battle)
    //     {
    //         if (opponentTeam.teamMates.Count == 0) { return; }
    //         var attackTarget = chooseAttackTarget(opponentTeam.teamMates, this);
    //         if (getDistanceSqToOpponent(attackTarget) < 1)
    //         {
    //             attack(attackTarget, 10);
    //             SetVelocity(1, 0, 0);
    //             return;
    //         }
    //         else
    //         {
    //             SetVelocity(attackTarget.tokenX - this.tokenX, attackTarget.tokenY - this.tokenY,5f);
    //             return;
    //         }
    //     }
    // }
}
